O banco pode ser criado via template ou migrations, funcionará com os dois. Única diferença seria que no template ja teria alguns dados.
Feito pelo Laravel, xampp, mysql.
PHP 8.2.12 
hosteado pelo artisan

compose install #instalar dependencias do composo
php artisan migrate #criar o banco pelo migrations (sem dados)
